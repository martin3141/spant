# Set the ppm reference value (eg ppm value at 0Hz).

Set the ppm reference value (eg ppm value at 0Hz).

## Usage

``` r
set_ref(mrs_data, ref)
```

## Arguments

- mrs_data:

  MRS data.

- ref:

  reference value for ppm scale.
