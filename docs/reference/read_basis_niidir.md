# Read a basis folder containing one NIfTI MRS file for each basis element.

Read a basis folder containing one NIfTI MRS file for each basis
element.

## Usage

``` r
read_basis_niidir(basis_dir)
```

## Arguments

- basis_dir:

  directory of basis files.

## Value

a basis_set object.
