# Extract the reference component from an mrs_data object.

Extract the reference component from an mrs_data object.

## Usage

``` r
get_ref(mrs_data)
```

## Arguments

- mrs_data:

  MRS data.

## Value

reference component.
