# Return the sampling frequency in Hz of an MRS dataset.

Return the sampling frequency in Hz of an MRS dataset.

## Usage

``` r
fs(mrs_data)
```

## Arguments

- mrs_data:

  MRS data.

## Value

sampling frequency in Hz.
