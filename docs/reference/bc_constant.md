# Remove a constant baseline offset based on a reference spectral region.

Remove a constant baseline offset based on a reference spectral region.

## Usage

``` r
bc_constant(mrs_data, xlim)
```

## Arguments

- mrs_data:

  MRS data.

- xlim:

  spectral range containing a flat baseline region to measure the
  offset.

## Value

baseline corrected data.
